// PrimePicks ARENA — Firebase project keys.
// Copy this file to config.js and fill in your own Firebase web app config
// (Firebase console → Project settings → Your apps → SDK setup and configuration).
// Web config keys are safe to expose in client code — Firestore security rules do the protecting.
// Leave the placeholders as-is to run in DEMO mode (fake players, no backend needed).
const FIREBASE_CONFIG = {
  apiKey: "PASTE_YOUR_API_KEY",
  authDomain: "PASTE_YOUR_PROJECT.firebaseapp.com",
  projectId: "PASTE_YOUR_PROJECT_ID",
};


